# API Keys for Lincoln Brief — copy to .env (gitignored)

# DART (전자공시시스템) — opendart.fss.or.kr 에서 발급 (빠름, 수 시간 내)
DART_API_KEY=

# KRX (한국거래소 OpenAPI) — openapi.krx.co.kr 에서 발급 (승인 약 1일)
KRX_API_KEY=

# (선택) Vercel 배포용 토큰 — vercel.com/account/tokens
VERCEL_TOKEN=
